-- IDs dos places da experiência Complexo: Breach.
-- Fonte única de verdade — usado pelo menu (gate de place) e pelo teleporte.
-- Ver docs/menu-plan.md.

return {
	StartPlaceId = 118472201586947, -- place inicial: menu / lobby
	FavelaPlaceId = 108947547992090, -- mapa da favela (destino do botão JOGAR)
}
